# 은닉층 8개 다층 퍼셉트론 학습 설정
learning_rate: 0.001
gamma: 0.99
epsilon_start: 1.0
epsilon_end: 0.1
epsilon_decay_start: 0.998
epsilon_decay_end: 1.0
num_episodes: 300000
max_steps_per_episode: 200
device: 'cuda'  # GPU 서버용
hidden_dim: 8  # 다층 퍼셉트론 은닉층 크기
num_hidden_layers: 8  # 은닉층 개수 